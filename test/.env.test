# Test credentials for the local Nextcloud instance in docker-compose.yml.
# These are NOT real credentials — safe to commit. Do not use against any
# production Nextcloud instance.

NEXTCLOUD_URL=http://nextcloud
NEXTCLOUD_USER=testadmin
NEXTCLOUD_PASSWORD=testpassword123
LOCAL_FOLDER=/sync
REMOTE_FOLDER=/

# Debug output
# VERBOSE=true
